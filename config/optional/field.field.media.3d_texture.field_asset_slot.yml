status: true
dependencies:
  config:
    - field.storage.media.field_asset_slot
    - media.type.3d_texture
id: media.3d_texture.field_asset_slot
field_name: field_asset_slot
entity_type: media
bundle: 3d_texture
label: 'Manifesto slot'
description: 'Which manifesto slot the asset binds to (e.g. case_diffuse, fan_idle_clip, stall_floor). Free-form so any property can author its own slot names.'
required: false
translatable: false
default_value: {  }
default_value_callback: ''
settings: {  }
field_type: string
